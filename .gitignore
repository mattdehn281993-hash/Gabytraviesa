# ---------------------------------------------------------------------------
# Client media IS committed, so the site works straight from a clone: her
# portrait on About, and the real recording the meditation player plays.
#
# NOTE: these are Gaby's own files — her recordings and her photographs. They
# are only appropriate to commit while this repo is private, or with her
# agreement. assets/misc/gaby-selfie-original.jpg in particular is a private
# snapshot, not a press photo.
# ---------------------------------------------------------------------------

# ---------------------------------------------------------------------------
# Internal documents — the briefs describe Gaby as a prospective client and
# carry commercial framing that is not hers to publish.
# ---------------------------------------------------------------------------
uploads/

# Editor / canvas scratch
.thumbnail
__*.html

# OS noise
.DS_Store
Thumbs.db
desktop.ini

# If a build ever appears
node_modules/
dist/
.env
.env.*
